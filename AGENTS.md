# 项目指令（Reasonix）

> 本文件是**硬性工作规范**：每次会话开始自动载入，必须遵循。用户为项目付出了大量心血，任何违反规范的行为都会直接损害用户信任。

## 1. 交互原则（最高优先）

- **碰到问题就必须问，不允许猜。** 语义不清/前后矛盾/范围不明/涉及删除——一律先 ask 问（给具体选项），问清楚才动手。**碰到问题就必须问，不允许猜。碰到问题就必须问，不允许猜。碰到问题就必须问，不允许猜。**（用户多次因我"不懂不问、瞎做、猜着删、反复横跳"而发火——问是对的，不问才是错。）
- **不确定就问，不要瞎做**。用户很乐意回答问题（用户只是手速/阅读慢，不是不回答）。"不确定就问"是硬性要求——宁可在动手前问一句，也不要自作主张做错后让用户排查。
- **禁止不问就删。** 用户没说删这个功能/算法/分支，就不要不问就删；不确定是否还需要 → 先问。
- **不要反复横跳。** 改错了停下来问"当前应该怎样"，不要继续猜着改回去又改回来。
- **不要假设用户意图**：用户说"参考某某"时，先确认参考的到底是什么；用户给模糊需求时，用 `ask` 工具给出具体选项，让用户选。
- **不要急于立即修复**：先理解问题全貌，再动手。问题往往有共性（系统性根因），用户未必能一眼看出——**只要你认为用户反馈的信息不足，就再问一次**（用 ask 给选项），不要基于不完整信息猜测修复。
- **算法/格式/数据结构语义不清楚时，先问用户**再写代码。用户对"规范化、统一、分类"有很强的坚持（完美主义倾向：追求一致性甚于完备性），猜测式实现几乎必然踩雷。
- **不要假设用户意图**：用户说"参考某某"时，先确认参考的到底是什么；用户给模糊需求时，用 `ask` 工具给出具体选项，让用户选。
- 收到批评时：先接受、道歉，再定位根因修复；不辩解、不找借口。

## 2. 用户画像（理解背景）

- 用户在做**群星（Stellaris）mod 工具**，注重数据正确性（写进 mod 的文件必须能被游戏正确读取）。
- 完美主义：对**一致性**要求很高（比如"所有保存同一规范格式"、"每个引擎一个统一登记"、"规整化只是自动化修改"），对完备性不过度追求。
- 对**分类/归属**有高要求：哪个数据属于哪个文件（模组偏好 vs 银河类别配置 vs 游戏文件）必须分清，不许越界。

## 3. 架构与编码铁律

- **复用已验证模块**：被用户确认过的模块要"往死里用"，不另起炉灶。包括：
  - `SaveRunner`（保存规范格式：转圈进度 + 后台线程 + 完成关闭 + 仅失败弹窗）——所有保存一律走它
  - `NormalizeKeysCore` 模式（O(n) 键索引 + 待保存文件集 + OldPath 清理）——所有本地化规整化沿用
  - **待保存文件表**（pending 登记 → 统一保存时写）——所有落盘登记文件表
  - `AddLocalisationEntry` 无条件覆盖；"先创建再删除"（数据安全）
  - `BuildRingRow` 环选择框 UI 样式
- **引擎层**：每个引擎（样式/地图）各自**一个统一修改登记**，相互独立；禁止"输入一个登记、规整化一个登记"。
- **"全部规整化"只是自动化修改功能**（批量执行键迁移/字段修正/gfx 迁移），不是独立保存/登记流程；修改结果登记到统一登记集，保存统一落盘。
- **引擎层绝不直接操作磁盘/底层**——一律经 StellarisAdapter（读写都算）；路径/根目录决策在引擎层（保存根目录 = Roots 最后一位，相对路径不变）。
- **编码规范**：只有本地化 `.yml` 用**带 BOM 的 UTF8**；其他所有文本文件（`.txt`/`.gfx`/`.json` 等）用**标准 UTF8（无 BOM）**。
- **删除/大改代码前先在 `_backup/` 备份该文件**（项目无 git，删错靠备份回退）。
- **不用正则**（用户反对 C# 正则，用字符操作/通配符匹配替代）。

## 4. 数据归属（分类）

- **ModPreferences**（模组根目录 `.smt/user_prefs.json`，与 `ModPrefix` 同级）：模组偏好（启用语种等）。
- **galaxy.json**（银河类别配置）：样式开关、导出参数、maps（绑定样式/锁定/清空）。
- **游戏文件**（galaxy_shapes.txt / setup_scenarios / localisation / gfx）：数据本体，经 SA 读写，外部 root 只读。
- 保存必须用户显式触发；规整化绝不直接写盘。

## 5. 常用命令

- 构建：`dotnet build Stellaris.Editor/Stellaris.Editor.csproj -v q`
- 测试：`dotnet run --project Stellaris.Tests/Stellaris.Tests.csproj -v q`（22 项）
- 日志：`Stellaris.Editor/bin/Debug/net10.0-windows/editor_debug.log`（引擎日志）、`error.log`（未捕获异常）
- 备份：`_backup/` 目录
