<!-- 保存进度弹窗：无边框、半透明、可拖动、带转圈加载动画（保存过程中显示） -->
<Window x:Class="Stellaris.Editor.SaveProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Topmost="True"
        ShowInTaskbar="True" Width="380" Height="150">
    <Border Background="#D9FFFFFF" BorderBrush="#9A9A9A" BorderThickness="1" CornerRadius="8"
            MouseLeftButtonDown="OnDrag">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- 转圈加载（旋转圆环动画） -->
            <Grid Width="32" Height="32" Margin="0,0,16,0">
                <Ellipse Width="26" Height="26" Stroke="#4A90D9" StrokeThickness="3.5"
                         StrokeDashArray="2.8,2.2" StrokeDashCap="Round"
                         RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <RotateTransform x:Name="SpinnerRotate" Angle="0" />
                    </Ellipse.RenderTransform>
                </Ellipse>
            </Grid>
            <TextBlock x:Name="ProgressText" FontSize="14" Foreground="Black"
                       VerticalAlignment="Center" TextWrapping="Wrap" MaxWidth="280" />
        </StackPanel>
    </Border>
</Window>
